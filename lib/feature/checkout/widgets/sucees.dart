import 'package:flutter/material.dart';

class Sucees extends StatelessWidget {
  const Sucees({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}